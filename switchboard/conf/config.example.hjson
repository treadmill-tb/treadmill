database: {
    address: localhost
    port: 5400
    auth: { PasswordAuth: {
        username: postgres
        password: ""
    } }
}
log_dir: switchboard/logs
server: {
    socket_addr: "0.0.0.0:443"
    cert: switchboard/conf/tls-certs/cert.pem
    key: switchboard/conf/tls-certs/key.pem
}
