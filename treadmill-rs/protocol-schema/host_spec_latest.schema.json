{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "HostSpecV2",
  "description": "Version 2 of the host spec.",
  "type": "object",
  "properties": {
    "description": {
      "description": "What this host is, in prose.",
      "type": [
        "string",
        "null"
      ]
    },
    "duts": {
      "description": "The devices under test wired to this host, in the order the operator\nlisted them. May be empty.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/DutV2"
      }
    },
    "gpio_controllers": {
      "description": "The host's GPIO controllers, keyed by a name the DUTs' pins refer to,\ne.g. `rp1`. May be empty.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/GpioController"
      }
    },
    "id": {
      "description": "Must equal the `host_id` of the host this document describes.",
      "type": "string",
      "format": "uuid"
    },
    "labels": {
      "description": "Operator-defined labels. CEL map indexing errors on an absent key, so\npredicates guard with `'key' in host.labels`.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "location": {
      "description": "Where in the site, e.g. `rack4/shelf2`. Free text.",
      "type": [
        "string",
        "null"
      ]
    },
    "name": {
      "description": "Display handle, e.g. `cam-rpi5-01`. Deliberately not unique; nothing\nroutes on it.",
      "type": "string"
    },
    "platform": {
      "$ref": "#/$defs/Platform"
    },
    "resources": {
      "$ref": "#/$defs/Resources"
    },
    "site": {
      "description": "The site the host lives at, e.g. `cambridge`. Flat, so a predicate reads\n`host.site == 'cambridge'`.",
      "type": "string"
    },
    "spec_version": {
      "$ref": "#/$defs/SpecVersionV2"
    }
  },
  "additionalProperties": false,
  "required": [
    "spec_version",
    "id",
    "name",
    "site",
    "platform",
    "resources",
    "labels",
    "gpio_controllers",
    "duts"
  ],
  "$defs": {
    "Console": {
      "description": "The board's console, as the host sees it.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "baud": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "device": {
              "description": "The host-side device node. Prefer a stable `/dev/serial/by-id/...`\npath over a `/dev/ttyACM*` name, which is enumeration-order\ndependent.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "uart"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "device",
            "baud"
          ]
        }
      ]
    },
    "DebugAccess": {
      "description": "How the board is programmed and debugged.",
      "type": "object",
      "properties": {
        "probe": {
          "$ref": "#/$defs/DebugProbe"
        },
        "protocol": {
          "description": "The wire protocol, e.g. `swd`, `jtag`. Governed by convention, not a\nregistry.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "protocol",
        "probe"
      ]
    },
    "DebugProbe": {
      "description": "The debug probe attached to a board.",
      "type": "object",
      "properties": {
        "model": {
          "description": "e.g. `J-Link OB`, `ST-LINK/V2-1`.",
          "type": "string"
        },
        "serial": {
          "description": "The probe's own serial, which is how a host-side tool addresses it.",
          "type": [
            "string",
            "null"
          ]
        },
        "vendor": {
          "description": "e.g. `SEGGER`, `STMicroelectronics`.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "vendor",
        "model"
      ]
    },
    "DutV2": {
      "description": "One device under test wired to a host.",
      "type": "object",
      "properties": {
        "arch": {
          "description": "The architectures of the board's cores, e.g. `cortex-m4`. An array\nbecause a heterogeneous-core part has more than one. May be empty.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "board": {
          "description": "The board this is, as a lowercase identifier, e.g. `nrf52840dk`.",
          "type": "string"
        },
        "connectivity": {
          "description": "What the board can talk over, e.g. `ble`, `ieee802154`, `usb`, `wifi`,\n`ethernet`, `can`. Governed by convention, not a registry. May be empty.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "console": {
          "anyOf": [
            {
              "$ref": "#/$defs/Console"
            },
            {
              "type": "null"
            }
          ]
        },
        "debug": {
          "anyOf": [
            {
              "$ref": "#/$defs/DebugAccess"
            },
            {
              "type": "null"
            }
          ]
        },
        "gpio": {
          "description": "The board's pins wired to a GPIO controller, keyed by the board's own\npin name, e.g. `P0.13`. May be empty.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/GpioPin"
          }
        },
        "labels": {
          "description": "As [`HostSpecV2::labels`], scoped to this DUT.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "name": {
          "description": "Display name, e.g. `nRF52840-DK`.",
          "type": [
            "string",
            "null"
          ]
        },
        "serial": {
          "description": "The board's **own** serial number, not its debug probe's.",
          "type": [
            "string",
            "null"
          ]
        },
        "vendor": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "vendor",
        "board",
        "arch",
        "connectivity",
        "gpio",
        "labels"
      ]
    },
    "GpioActive": {
      "description": "The level at which a DUT pin's function is asserted.",
      "type": "string",
      "enum": [
        "low",
        "high"
      ]
    },
    "GpioController": {
      "description": "A GPIO controller on the host, e.g. a SoC's pin controller or a USB GPIO\nexpander.",
      "type": "object",
      "properties": {
        "config": {
          "description": "Driver-specific settings that locate the controller.",
          "type": "object",
          "additionalProperties": true
        },
        "driver": {
          "description": "How the host drives this controller, e.g. `linux-gpiochip`. Governed by\nconvention, not a registry.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "driver",
        "config"
      ]
    },
    "GpioDrive": {
      "description": "The output drive at a DUT pin.\n\nAn NPN transistor to ground, for example, only ever pulls the pin low.\n\nA job writes the level it wants at the DUT pin. The supervisor sets the\nhost line to that value, or to its opposite if the pin is `inverted`. The\nDUT pin then ends up as follows:\n\n| drive         | inverted | job writes | host line set to | DUT pin             |\n|---------------|----------|------------|------------------|---------------------|\n| `push_pull`   | `false`  | 0          | 0                | DUT pin is low      |\n| `push_pull`   | `false`  | 1          | 1                | DUT pin is high     |\n| `push_pull`   | `true`   | 0          | 1                | DUT pin is low      |\n| `push_pull`   | `true`   | 1          | 0                | DUT pin is high     |\n| `open_drain`  | `false`  | 0          | 0                | DUT pin is low      |\n| `open_drain`  | `false`  | 1          | 1                | DUT pin is released |\n| `open_drain`  | `true`   | 0          | 1                | DUT pin is low      |\n| `open_drain`  | `true`   | 1          | 0                | DUT pin is released |\n| `open_source` | `false`  | 0          | 0                | DUT pin is released |\n| `open_source` | `false`  | 1          | 1                | DUT pin is high     |\n| `open_source` | `true`   | 0          | 1                | DUT pin is released |\n| `open_source` | `true`   | 1          | 0                | DUT pin is high     |\n\nA released DUT pin floats, unless the DUT pulls it up or down.",
      "oneOf": [
        {
          "description": "The host can drive the DUT pin high and low. Examples: a direct wire,\nwith or without a series resistor, or a push-pull buffer or inverter.",
          "type": "string",
          "const": "push_pull"
        },
        {
          "description": "The host can pull the DUT pin low or release it. Examples: an NPN\ntransistor or N-MOSFET to ground, an open-drain buffer, a diode with its\ncathode towards the host, or a direct wire that another part of the\nboard also pulls low.",
          "type": "string",
          "const": "open_drain"
        },
        {
          "description": "The host can drive the DUT pin high or release it. Examples: a PNP\ntransistor or P-MOSFET to the supply, an open-source buffer, a diode\nwith its anode towards the host, or a direct wire that another part of\nthe board also drives high.",
          "type": "string",
          "const": "open_source"
        }
      ]
    },
    "GpioPin": {
      "description": "One DUT pin wired to a host GPIO controller.\n\n# Examples\n\nOther fields are left out below.\n\nAn LED on the DUT board, which the DUT turns on by pulling its pin low. The\nhost reads the pin:\n\n```json\n\"LED1\": {\n  \"modes\": [\"digital_in\"],\n  \"active\": \"low\",\n  \"inverted\": false,\n  \"drive\": null,\n  \"note\": null\n}\n```\n\nA button on the DUT board that connects its pin to ground. The host wire\nconnects to the same pin and presses the button by pulling the pin low. The\nbutton can pull the pin low at the same time, so the host must never drive\nit high:\n\n```json\n\"BUTTON1\": {\n  \"modes\": [\"digital_out\"],\n  \"active\": \"low\",\n  \"inverted\": false,\n  \"drive\": \"open_drain\",\n  \"note\": null\n}\n```\n\nA Pico 2's `RUN` line, pulled to ground by an NPN transistor whose base the\nhost drives. Asserting the pin holds the board in reset. Releasing it lets\nthe Pico's pull-up take `RUN` high:\n\n```json\n\"RUN\": {\n  \"modes\": [\"digital_out\"],\n  \"active\": \"low\",\n  \"inverted\": true,\n  \"drive\": \"open_drain\",\n  \"note\": \"NPN, 1k base\"\n}\n```\n\nA general-purpose DUT pin that the host can read and drive. A series\nresistor limits the current when the host and the DUT drive the pin to\nopposite levels:\n\n```json\n\"P1.01\": {\n  \"modes\": [\"digital_in\", \"digital_out\"],\n  \"active\": null,\n  \"inverted\": false,\n  \"drive\": \"push_pull\",\n  \"note\": \"1k series\"\n}\n```",
      "type": "object",
      "properties": {
        "active": {
          "description": "The DUT pin's function is asserted at this level, e.g. `low` for a reset\nline or a button to ground. This is a property of the board. `null`\nmeans the pin has no asserted level.",
          "anyOf": [
            {
              "$ref": "#/$defs/GpioActive"
            },
            {
              "type": "null"
            }
          ]
        },
        "config": {
          "description": "Driver-specific settings that locate the pin on its controller.",
          "type": "object",
          "additionalProperties": true
        },
        "controller": {
          "description": "The key of the controller in `gpio_controllers`.",
          "type": "string"
        },
        "drive": {
          "description": "How the host can drive the DUT pin. Set only on pins with a\n`digital_out` mode.",
          "anyOf": [
            {
              "$ref": "#/$defs/GpioDrive"
            },
            {
              "type": "null"
            }
          ]
        },
        "inverted": {
          "description": "Set when the wiring inverts the signal, e.g. through an inverter or a\ntransistor. The DUT pin then sits at the opposite level of the host\nline. The supervisor applies the inversion, so jobs always see the DUT\npin's level.",
          "type": "boolean",
          "default": false
        },
        "label": {
          "description": "What the pin is for on the board, e.g. `LED1`, `BUTTON1`.",
          "type": [
            "string",
            "null"
          ]
        },
        "modes": {
          "description": "How the host may use the pin: `digital_in`, `digital_out`. Governed by\nconvention, not a registry.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "note": {
          "description": "Human-readable notes, e.g. `NPN, 1k base` or `470Ω series`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "modes",
        "controller",
        "config"
      ]
    },
    "Platform": {
      "description": "The machine a host is, and the images it can boot.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "arch": {
              "description": "The host's own CPU architecture, e.g. `aarch64`, `x86_64`.\n\nRedundant — a profile implies it — but kept so \"any aarch64 host\"\ndoes not require enumerating profiles. Nothing enforces consistency\nbetween the two.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "physical"
            },
            "model": {
              "type": "string"
            },
            "profiles": {
              "description": "The whole machine configurations this host can boot, e.g.\n`rpi4-uboot-sd`, `q35-virtio-uefi`, `q35-virtio-bios`,\n`netboot-nbd`. A profile names a complete configuration the way a\ntarget triple does; an image set member matches one by equality.\n\nAn array because one host may genuinely serve several. Governed by\nconvention, not a registry.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "vendor": {
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "arch",
            "profiles",
            "vendor",
            "model"
          ]
        },
        {
          "type": "object",
          "properties": {
            "arch": {
              "description": "The architecture the guest is presented with.",
              "type": "string"
            },
            "hypervisor": {
              "description": "e.g. `qemu`.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "virtual"
            },
            "profiles": {
              "description": "As [`Platform::Physical::profiles`].",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "kind",
            "arch",
            "profiles",
            "hypervisor"
          ]
        }
      ]
    },
    "Resources": {
      "description": "The ceiling available to a single job on this host.\n\nUnsigned, so CEL sees these as `uint`: comparisons against a plain literal\nwork (`host.resources.memory_mb >= 4096`), but arithmetic needs an unsigned\nliteral (`host.resources.memory_mb / 1024u >= 8`).",
      "type": "object",
      "properties": {
        "cpu_cores": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "memory_mb": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "storage_gb": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "cpu_cores",
        "memory_mb",
        "storage_gb"
      ]
    },
    "SpecVersionV2": {
      "description": "The `spec_version` discriminant of a [`HostSpecV2`] document.",
      "type": "string",
      "enum": [
        "v2"
      ]
    }
  }
}
